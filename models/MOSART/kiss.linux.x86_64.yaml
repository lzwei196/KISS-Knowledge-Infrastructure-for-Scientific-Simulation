kiss_manifest_version: 1
model: MOSART
binary_type: Python
verified: unverified
install_dir: MOSART
acquire:
  strategy: pip
  package: mosartwmpy==0.6.2
  produces: mosartwmpy.model
python_deps:
- setuptools<81
notes: 'LINUX x86_64 port of the macos recipe (kiss.macos.arm64.yaml): same source
  pin and product path; binary_type/system_deps adapted. Not yet verified on Linux.
  Verifies the KI-declared PNNL MOSART-WM Python implementation, not a standalone
  Fortran MOSART binary. The baseline acquired0.6.2; KI descriptive1.0.x label is
  not a verified distribution version. Fresh import verification pending.'
agent_hint: Install official mosartwmpy0.6.2 with full dependencies and setuptools<81
  for pkg_resources. Verify mosartwmpy.model.Model can be imported without instantiation,
  run pip check, preserve workspace interpreter. No simulation or scientific input
  acquisition.
system_deps: []
