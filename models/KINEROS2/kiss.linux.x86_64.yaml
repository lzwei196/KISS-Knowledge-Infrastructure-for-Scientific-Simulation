kiss_manifest_version: 1
model: KINEROS2
binary_type: Python
verified: unverified
install_dir: KINEROS2
acquire:
  strategy: bundled
  produces: tools/run_kineros2.py
python_script:
  implementation_id: hydrocraft-kineros2-lumped
  path: tools/run_kineros2.py
  sha256: e7be86c74e3d1d2fcff599230a551611a4c6af1fb04c5e63714c045e52f9c300
python_deps:
- numpy
- pandas
- scipy
- xarray
- geopandas
- shapely
- matplotlib
notes: 'LINUX x86_64 port of the macos recipe (kiss.macos.arm64.yaml): same source
  pin and product path; binary_type/system_deps adapted. Not yet verified on Linux.
  Installs the declared hydrocraft-kineros2-lumped Python implementation (1.0.0).
  This is not installation of the official upstream KINEROS2 application and does
  not establish algorithmic equivalence. The original scientific runner bytes are
  unchanged. Fresh DS installation passed the source-bound declared-variant contract
  on 2026-09-09. Official upstream installation remains unverified.'
agent_hint: Read docs/install.macos.md. Use the actual bundled ki/tools/run_kineros2.py
  in the workspace; do not create a replacement runner, copy a stub, or choose the
  Python interpreter as model product. Install genuine listed dependencies into the
  workspace venv. The fixed python_script gate verifies implementation identity and
  runner SHA256, then requires --help exit0 in an empty directory with no created
  paths. Do not run --version (unsupported), construct model objects, call simulation/calibration
  functions, invoke scientific preflight checks, or create/download input data. No
  official upstream application is required for this explicitly declared variant contract.
system_deps: []
