kiss_manifest_version: 1
model: PyMT
binary_type: Python
verified: unverified
install_dir: PyMT
acquire:
  strategy: pip
  package: pymt==1.3.2
  produces: pymt.framework
python_deps:
- setuptools<81
notes: 'LINUX x86_64 port of the macos recipe (kiss.macos.arm64.yaml): same source
  pin and product path; binary_type/system_deps adapted. Not yet verified on Linux.
  Installation contract verifies the official PyMT modeling framework and native dependencies;
  individual BMI model plugins and scientific runs are separate. Native conda-forge1.3.2
  environment with Python3.11 and numpy1.x was provisioned and imports locally passed;
  fresh DS verification pending.'
agent_hint: Preserve the preprovisioned genuine workspace conda environment containing
  pymt1.3.2, native ESMF/esmpy and UDUNITS. Do not widen dependency constraints or
  replace compiled components. setuptools<81 supplies required pkg_resources. Verify
  pymt.framework, pymt.units and esmpy imports, plus pip check. This is framework
  readiness only; do not claim BMI model plugins installed or run simulations.
system_deps: []
