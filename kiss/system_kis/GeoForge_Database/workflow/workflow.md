# GeoForge Database task workflow

`user goal → scoped query → authenticated Desktop catalogue adapter → exact
candidate records → user data selection → Desktop-owned approved download`

The KI covers the first four nodes. GeoForge Desktop owns authentication,
selection state, checksum verification, download receipts, and any manual
delivery request. The Agent never receives the persistent activation token.
