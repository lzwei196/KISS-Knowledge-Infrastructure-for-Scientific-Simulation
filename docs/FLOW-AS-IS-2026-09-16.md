# GeoForge Desktop: how one chat turn actually runs today (as-is, from code)

Read on 2026-09-16 from `kiss/kiss_cli/gui.py` (chat handler), `kiss/kiss_cli/flowrun.py`
(`pre`, `turn`, `after`), `ki_tools_common/.../flow/states.py` (state table), `policy.py`
(tools per state), `kiss/kiss_cli/obs_subset.py` + `data_proposal.py` (subset side path).
This is what the code does, not what we want it to do.

## 1. The main loop: one user message

```mermaid
flowchart TD
    U[User message or card click] --> P0{Pending card with an action?}
    P0 -- generic card: choice, download-in-place, setup --> R0[resolve the card] --> PRE
    P0 -- plan-approval card --> PRE
    P0 -- no --> PRE

    PRE[flowrun.pre] --> S{flow state}
    S -- NEW and text is not a scientific task --> CHAT[plain chat, no flow]
    S -- NEW --> RES[RESOLVING_KIS]
    RES -- no KI pinned --> INTAKE
    RES -- KI pinned --> PLN
    S -- WAITING_FOR_USER, click approve --> APPR
    S -- WAITING_FOR_USER, click modify --> PLN
    S -- WAITING_FOR_USER, text names a KI --> PLN
    S -- PLANNING / REPLAN_REQUIRED --> PLN
    S -- EXECUTING --> EXE
    S -- FAILED / FAILED_VALIDATION, approval still valid --> RERUN[rerun to APPROVED then EXECUTING] --> EXE
    S -- SETUP_REQUIRED / SETUP_RUNNING --> SETUP[setup turn]
    S -- PLAN_REVIEW / APPROVED / VERIFYING / COMPLETED --> RO[read-only Q and A turn]

    INTAKE[intake turn: agent reads KI catalogue, may search DB] --> IM{how did it end?}
    IM -- report_project_progress marker with selected KIs --> PROMO[promote_auto_choice] --> PLN2[planning turn starts in the same request]
    IM -- request_user_action --> CARD1[choice card in chat, state stays RESOLVING_KIS] --> U
    IM -- prose only --> DEAD1[nothing happens, state stays RESOLVING_KIS] --> U

    PLN[planning turn] --> PB[app derives draft plan and inventory, strips server-era ids] --> PC[contract + study hints + PLAN HANDOFF + last validation errors] --> AG1[agent turn with write_plan and search_observation_data]
    PLN2 --> PB
    AG1 --> AFT1[flowrun.after PLANNING]
    AFT1 --> A1{result}
    A1 -- provider failed --> F1[message: retry planning. No auto retry] --> U
    A1 -- no write_plan this turn --> F2[message: not submitted. Repair only if provider succeeded] --> U
    A1 -- validation errors or unknown DB ids --> REP[repair round: same turn, errors fed back, loop until same draft fails twice]
    REP --> AG1
    A1 -- valid --> CARD2[PLAN_REVIEW to WAITING_FOR_USER: approval card with data groups, steps, blockers] --> U

    APPR[approve: sign approval, bind any downloaded subsets, APPROVED] --> SU{KI software verified?}
    SU -- yes --> EXE
    SU -- no --> SETUP
    SETUP --> SV[setup verified] --> EXE

    EXE[execution turn: contract + tools run_ki_tool, download_observation_data, request_replan] --> AG2[agent turn] --> AFT2[flowrun.after EXECUTING]
    AFT2 --> E1{result}
    E1 -- REPLAN marker or request_replan --> REV[revoke approval, REPLAN_REQUIRED] --> AUTORE[auto replan turn in the same request] --> AG1
    E1 -- validation failed --> FV[FAILED_VALIDATION] --> U
    E1 -- every step has a receipt --> DONE[COMPLETED]
    E1 -- still running, manual download waiting --> MSG1[chat reminder: link is in Project status] --> U
    E1 -- still running, zero receipts this turn --> MSG2[chat line: receipts 0 of N] --> U
    E1 -- still running --> U
```

## 2. The data side path (runs beside the state machine, not inside it)

```mermaid
flowchart TD
    subgraph agent [Agent, during intake or planning, one tool search_observation_data]
        T1[query / bbox / period / variable: local catalogue search]
        T2[describe_dataset_id: live file schema]
        T3[resolve_dataset_id: split-product children]
        T4[subset_request: read-only clip estimate] --> ST[.geoforge/subsets/id.json: awaiting_approval / needs_review / estimate_failed]
        T5[subset_proposal: list of estimate ids + purposes] --> PROP[.geoforge/data-proposal.json, revision, acknowledged=false]
    end

    PROP --> PS[Project status: Data proposed for this project, popup once per revision]
    PS --> B1[button Approve selected acquisitions] --> J[server job per item, status queued] --> POLL[panel polls, refresh] --> RDY[ready]
    PS --> B2[button Refresh estimates, only when 15-min TTL passed] --> ST
    PS --> B3[button Revise data selection] --> agent
    RDY --> B4[button Download and check all files] --> DL[manifest checks: parts, sizes, sha, versions, scope] --> INS[inspect NetCDF / TIFF] --> RCPT[signed acquisition receipt, status downloaded]
    RCPT --> BIND{plan approved and an inventory item carries this acquisition_id and a step consumes it?}
    BIND -- yes --> DR[data receipt bound to the plan step]
    BIND -- no --> WAIT[stays downloaded, unbound, until plan approval or execution]

    subgraph plan [Plan side]
        WP[agent write_plan: inventory item must carry dataset_id, acquisition_id, requirements] --> STAMP[stamp_inventory: id must exist in catalogue, acquisition scope must match item, parent products refused] --> CARDX[approval card]
        CARDX --> APPROVE[user approves plan] --> BINDALL[bind_approved for every downloaded subset]
        APPROVE --> EXEC[EXECUTING: download_observation_data per item]
        EXEC -- acquisition_id --> ADV[advance_approved: refresh, download, bind] --> DR
        EXEC -- served --> SRV[direct download with receipt]
        EXEC -- manual --> HAND[download request card: Baidu link, code, target path, run waits] --> USR[user places files, clicks Files are in place] --> CHK[download_placed: files exist?] --> SRV
    end
```

## 3. Exact sequence, in words

1. A message arrives. If a generic card is pending and the message carries the click, the click is resolved first. The plan-approval card is not resolved here; it is passed to the flow.
2. `pre()` looks at the state. On an approve click it signs the approval, binds any subset already downloaded, moves to APPROVED, and to EXECUTING if the KI software is verified, otherwise to SETUP_REQUIRED. On a modify click it goes back to PLANNING with the note as the reason.
3. NEW with a non-scientific message is plain chat. NEW with a scientific message becomes RESOLVING_KIS. With no KI pinned, an intake turn runs. The agent must end it with a hidden marker naming the KI, or with a choice card. If it ends with prose, nothing happens and the next message starts intake again.
4. Once a KI is chosen, planning starts inside the same request. The app derives a draft plan and inventory, feeds the contract, the study hints from the local catalogue, the "save both files" instruction, and the last validation errors. CLI providers get a scratch copy of the project. API providers get `write_plan`.
5. `after()` for planning: provider failure prints "retry planning" and stops. No `write_plan` prints "not submitted" and, only if the provider did not fail, starts a repair round. Validation errors and unknown catalogue ids start repair rounds until the same draft fails twice. A valid plan produces the approval card and the state becomes WAITING_FOR_USER. There is no auto-approval.
6. Execution turns get the execution contract and the run tools. `after()` for execution: a replan marker revokes the approval and starts a replan turn immediately. Validation failure lands in FAILED_VALIDATION; the next user message reruns under the same approval. All receipts present ends in COMPLETED. Otherwise the chat gets a reminder if a manual download is waiting, or a "0 receipts" line if the agent ran nothing.
7. The subset path is separate. The agent can estimate any time it has the search tool, publish a proposal, and the user gets a second approval in Project status. Jobs, downloads and receipts happen there. They join the plan only if the agent copied the acquisition id into the inventory item and the plan is approved.

## 4. Where the seams are (facts, not opinions)

- Two approvals: plan card in chat, acquisition button in Project status. Either can come first.
- The join between them is a string the agent copies by hand (`acquisition_id`).
- Estimates expire after 15 minutes; a refresh creates new ids, which the plan does not know.
- One tool, `search_observation_data`, has five modes chosen by which optional field is filled.
- An intake or planning turn that ends in prose is not an error to the agent; it is only reported to the user afterwards.
- A provider timeout in planning is reported, not retried.
- Data status is read from five places: subset state files, `data-proposal.json`, `data-inventory.json`, `inventory-updates.jsonl`, receipts.
- Project status has six data sections: needs-you request, proposal, active jobs, exploration history, manual estimate form, data in this plan.
